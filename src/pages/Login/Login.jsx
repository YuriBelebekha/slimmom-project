import { Helmet, HelmetProvider } from 'react-helmet-async';

const Login = () => {
  return (
    <HelmetProvider>
      <Helmet>
        <title>SlimMom | Login</title>
      </Helmet>

      
    </HelmetProvider>
  )
};

export default Login;